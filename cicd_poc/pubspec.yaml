name: cicd_poc
description: A Flutter playground project to demonstrate CI/CD on a pipe pipeline.

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=2.17.3 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  test: ^1.21.1
  build_runner: ^2.2.0
  flutter_test:
    sdk: flutter
  integration_test:
    sdk: flutter
  firebase_core: ^2.3.0

  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
